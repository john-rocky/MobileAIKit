// Placeholder source so SPM has something to declare a target around.
// CI only runs `xcodebuild -resolvePackageDependencies` on this fixture,
// which validates the manifest graph without compiling any code.
import AIKit
